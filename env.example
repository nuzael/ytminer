# Copy to .env and fill in values
YOUTUBE_API_KEY=

# Defaults for interactive mode
YTMINER_DEFAULT_REGION=any
YTMINER_DEFAULT_DURATION=any
YTMINER_DEFAULT_TIME_RANGE=30d
YTMINER_DEFAULT_ORDER=relevance

# Transcript settings
# Enable transcript fetching (true/false)
YTMINER_WITH_TRANSCRIPTS=false
# Comma-separated list of preferred languages for transcripts
YTMINER_TRANSCRIPT_LANGS=en,pt

# Opportunity Score Weights (optional overrides)
# Defaults: VPD=0.45, LIKE=0.25, FRESH=0.20, SAT=0.30, SLOPE=0.15
# YTMINER_OPP_W_VPD=0.45
# YTMINER_OPP_W_LIKE=0.25
# YTMINER_OPP_W_FRESH=0.20
# YTMINER_OPP_W_SAT=0.30
# YTMINER_OPP_W_SLOPE=0.15

# Velocity/Keyword thresholds (optional)
# A channel is a Rising Star if its AvgVPD > (multiplier * niche AvgVPD)
YTMINER_RISING_STAR_MULTIPLIER=1.5
# A keyword is considered Long Tail if Frequency <= max_freq AND Avg Engagement > min_engagement (%)
YTMINER_LONG_TAIL_MIN_ENGAGEMENT=5.0
YTMINER_LONG_TAIL_MAX_FREQ=2

# Note: All settings can be configured through the interactive interface
# Run: ./ytminer.exe
